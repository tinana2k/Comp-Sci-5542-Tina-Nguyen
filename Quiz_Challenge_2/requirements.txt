# ── Core Speech Transcription ─────────────────────────────────────────────────
openai-whisper>=20231117          # OpenAI Whisper STT (tiny / base / small / medium)

# ── Alternative STT Model ─────────────────────────────────────────────────────
# facebook/wav2vec2-base-960h — loaded via transformers
transformers>=4.40.0              # Hugging Face Transformers (Whisper, wav2vec2, BART, DistilBERT, MarianMT, SpeechT5)
accelerate>=0.30.0                # Mixed-precision & device-map utilities
sentencepiece>=0.2.0              # Tokeniser for MarianMT / mBART translation models
datasets>=2.19.0                  # HF Datasets (used by SpeechT5 speaker embeddings)

# ── Text-to-Speech (SpeechT5) ─────────────────────────────────────────────────
# microsoft/speecht5_tts — loaded via transformers
# speaker embeddings: Matthijs/cmu-arctic-xvectors

# ── Deep Learning Runtime ─────────────────────────────────────────────────────
torch>=2.1.0                      # PyTorch (CUDA-enabled for GPU inference)
torchaudio>=2.1.0                 # Audio I/O, resample, feature extraction

# ── Audio Processing ─────────────────────────────────────────────────────────
librosa>=0.10.1                   # MFCC, spectrogram, audio feature extraction
soundfile>=0.12.1                 # Read/write WAV / FLAC audio
ffmpeg-python>=0.2.0              # FFmpeg wrapper (MP3 → WAV conversion)

# ── NLP / LLM Analysis ───────────────────────────────────────────────────────
nltk>=3.8.1                       # Tokenisation, stopwords
rouge-score>=0.1.2                # ROUGE-1 / ROUGE-2 / ROUGE-L (summarisation)
sacrebleu>=2.4.0                  # BLEU score (translation quality)
keybert>=0.8.3                    # BERT-based keyphrase extraction (KeyBERT + MMR)
scikit-learn>=1.4.0               # TF-IDF baseline keyword extraction

# ── Data / Persistence ───────────────────────────────────────────────────────
pandas>=2.2.0                     # Tabular results & metric tables
numpy>=1.26.0                     # Numerical operations

# ── Visualisation ────────────────────────────────────────────────────────────
matplotlib>=3.8.4                 # Evaluation charts and plots

# ── Notebook / Reporting ─────────────────────────────────────────────────────
ipykernel>=6.29.0                 # Jupyter kernel
ipywidgets>=8.1.2                 # Interactive notebook widgets
gtts>=2.5.1                       # Google TTS — generate synthetic demo audio (CPU)
